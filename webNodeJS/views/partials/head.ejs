<script>
    // Obtenemos los datos desde el servidor
    var localStorageData = <%- JSON.stringify(localStorageData) %>;

    // Guardamos los datos en localStorage
    localStorage.setItem('apiEndpoint', localStorageData.apiEndpoint);

    console.log('Datos guardados en localStorage:', localStorageData);
  </script>
<meta name="viewport" content="width=device-width, initial-scale=1">
<!-- Importamos los estilos de Bootstrap -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/css/bootstrap.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
<link rel="stylesheet" href="/css/style.css">